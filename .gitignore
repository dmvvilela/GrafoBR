# Node / Next
node_modules/
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Env
.env
.env.local
!.env.example

# Generated data (one file per politician). Keep the folder + its README only.
data/*
!data/.gitkeep
!data/README.md

# Pipeline output synced into the frontend by web/scripts/sync-data.mjs (generated).
# Keep only the synthetic sample fixture.
web/public/data/*.json
!web/public/data/sample-ego-network.json

# Reference clones (br-acc etc.) — study material, never shipped. Keep the README.
reference/*
!reference/README.md

# Caches / OS
.duckdb/
*.duckdb
.cache/
.DS_Store
